New-session summary:

We are fixing Part D.4 EGN in GCO-HPIF. The bug is in src/gco_hpif/solvers/egn_solver.py, function build_extra_pyg_infer_datasets(...). For extra all-test stores such as COLLAB and IMDB-BINARY, it currently writes graph_id values like collab_000000 and source_index=0. The rest of the repo uses canonical graph IDs like collab_graph000001 and source_index=1. Patch egn_solver.py so extra graph IDs are canonical, rerun EGN inference with the existing checkpoint to artifacts/solver_runs/egn_full_all_test_graphs_fixed, verify IDs, then rerun Part E strict hardness labeling using the fixed EGN solver_runs.csv.